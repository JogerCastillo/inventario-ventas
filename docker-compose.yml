services:
  backend:
    build:
      context: ./backend
    env_file:
      - backend/.env
    environment:
      PORT: 4100
    ports:
      - "4100:4100"

  frontend:
    build:
      context: .
    depends_on:
      - backend
    ports:
      - "8080:80"
